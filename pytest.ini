[pytest]
testpaths = tests
# Put the project root on sys.path so `app.*` and `tests.conftest` import the
# same way whether the suite is run as `pytest` or `python -m pytest`.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
