dash>=2.17
pandas>=2.0
plotly>=6,<8
requests
beautifulsoup4
lxml
openpyxl
geopy
selenium
webdriver-manager
pywebview
waitress
# Overview briefing PDF: kaleido renders Plotly figures to PNG; reportlab
# assembles the PDF (pure Python, no system libraries). plotly>=6 requires
# kaleido>=1, which no longer bundles Chromium — it drives the system Chrome
# the app already needs for the power scraper (installed in the Dockerfile).
kaleido>=1,<2
reportlab>=4.0
# Storm tracker: OpenCV (headless — no GUI libs needed in Docker) for radar
# echo detection + annotation; numpy comes with it but is used directly too.
opencv-python-headless>=4.8,<5
numpy>=1.24
# Operational Summary (Intel Tool): fills the SCC's own .pptx template.
python-pptx>=1.0,<2
# werkzeug ships with dash/flask; pinned here as it backs the admin password hash.
werkzeug>=2.0
